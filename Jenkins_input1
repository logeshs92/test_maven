pipeline {
  agent any
  stages {
  stage('Choose environment') {
  	steps {
  		userInput = input(id: 'userInput', message: 'Choose an environment', parameters: [[name: 'NPM_INSTALL', choices: ['yes','no'].join('\n'), description: 'Please select the Environment']])
  	}
  }
  stage('test') {
	steps {
		sh label: '', script: '''
		if [ userInput.Env -eq yes ];
		then 
		echo "yes"
		else
		echo "no"
		fi'''
  	  }
   	}
  }
}
